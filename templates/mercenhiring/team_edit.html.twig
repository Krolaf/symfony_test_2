{% extends 'base.html.twig' %}

{% block content %}
<h1>Modifier l'équipe {{ team.name }}</h1>
<div class="content-form-container">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn">Enregistrer</button>
{{ form_end(form) }}
</div>
<a href="{{ path('team_list') }}">Retour à la liste des équipes</a>
{% endblock %}
